spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:sparta-postgres}:5432/${DB_NAME:sparta_logistics}?currentSchema=order_schema
    username: ${DB_USERNAME:sparta_admin}
    password: ${DB_PASSWORD:password123}
    driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true

    data:
      redis:
        host: ${REDIS_HOST:sparta-redis}
        port: 6379

app:
  cache:
    user-company-ttl: 3600